# Profile ativo: local (desenvolvimento) ou prod (Koyeb)
# Para produção, defina SPRING_PROFILES_ACTIVE=prod no Koyeb
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Import do arquivo de configuração específico do profile
spring.config.import=optional:application-${spring.profiles.active}.properties


