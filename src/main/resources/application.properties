# Active profile: local (development) or prod (Koyeb)
spring.config.import=optional:application-${spring.profiles.active}.properties
# Import profile-specific configuration file

spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
# For production, set SPRING_PROFILES_ACTIVE=prod in Koyeb

